# 实验报告模板

> 删除本行提示后提交。报告应简洁呈现证据与解释，不需要复述整份实验说明。字段单位、三轮汇总和绘图方法见[《结果分析指南》](结果分析指南.md)。

## 1. 实验环境与复现信息

| 项目 | 内容 |
|---|---|
| CPU |  |
| 内存 |  |
| SSD 型号与文件系统 |  |
| 操作系统与内核 |  |
| 编译器 |  |
| PipeANN Commit |  |
| GateANN Commit |  |
| I/O Backend | AIO |
| 公共参数 | threads=1, beamwidth=8, K=10 |

说明 Debug 构建和查询是否成功；列出源码或课程脚本修改。若没有修改，写“无”。

## 2. 系统设计理解

用一张简图分别标出 PipeANN-Filter 和 GateANN 从查询输入到 SSD 数据访问的关键步骤。回答：过滤发生在哪里，系统如何处理过滤导致的图不可达或无效 I/O，两种设计分别付出了什么 CPU、DRAM、SSD 空间或构建代价。

## 3. 实验方法

给出实际使用的数据 Tier、三档 Selectivity、L、重复次数和缓存/后台负载控制方法。说明为何两系统 L 数值不同，以及比较时如何结合 Recall 判断公平性。

## 4. 实验结果

### 4.1 Recall–Latency

插入 High workload 上的 Recall@10–Mean Latency 曲线，使用三轮中位数并显示范围或误差线。

| 系统 | L | Recall@10 | Mean Latency | QPS | Mean I/O |
|---|---:|---:|---:|---:|---:|
| PipeANN | 20 |  |  |  |  |
| PipeANN | 40 |  |  |  |  |
| PipeANN | 80 |  |  |  |  |
| GateANN | 160 |  |  |  |  |
| GateANN | 320 |  |  |  |  |
| GateANN | 640 |  |  |  |  |

解释搜索预算增加后 Recall、延迟、I/O 和内部行为的变化；在 Recall 相近的点上比较两套系统。

### 4.2 Selectivity–Performance

固定 PipeANN L=40、GateANN L=320，插入 Low/Medium/High 的 Recall、Mean Latency 和 Mean I/O 图。

| 系统 | Bucket | 实际 Selectivity | Recall@10 | Mean Latency | Mean I/O | 内部指标 |
|---|---|---:|---:|---:|---:|---:|
| PipeANN | Low |  |  |  |  |  |
| PipeANN | Medium |  |  |  |  |  |
| PipeANN | High |  |  |  |  |  |
| GateANN | Low |  |  |  |  |  |
| GateANN | Medium |  |  |  |  |  |
| GateANN | High |  |  |  |  |  |

解释过滤强度变化下两种系统的敏感性。明确真实标签分桶不是严格单变量实验。

### 4.3 资源 Trade-off

| 系统 | 索引大小 | 查询峰值 RSS | 关键额外结构 | 获得的收益 |
|---|---:|---:|---|---|
| PipeANN |  |  |  |  |
| GateANN |  |  |  |  |

注意 `peak_rss_kb` 是查询进程峰值，`elapsed_seconds` 包含加载和初始化，不能代替 Query Phase 延迟。

## 5. 机制与瓶颈分析

选择至少两个关键现象，分别按“测量结果 → 源码/论文机制 → 瓶颈判断”分析。区分直接测得的事实与推断，避免只描述图表趋势。

## 6. 优化方向

### Observation

引用具体实验数据。

### Mechanism

说明相关系统机制和代码位置。

### Bottleneck

说明瓶颈证据以及尚未验证的假设。

### Optimization

说明拟修改的阶段、策略或数据结构。

### Expected Benefit

说明预期改善的 workload 和指标。

### Trade-off

分析 CPU、DRAM、SSD 空间、构建时间或复杂度代价。

### Validation Plan

给出对照组、自变量、控制变量、指标、重复次数，以及支持或否定该优化的预期结果。

## 7. 结论

用简短段落总结两种设计最重要的性能差异、适用场景和优化判断。

## 附录：复现材料

列出提交包内 `commands.txt`、环境记录、聚合结果、原始日志和绘图脚本的位置。
